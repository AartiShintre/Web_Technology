let str = "123";
let num = Number(str);   // string → number
console.log(num, typeof num);

let bool = Boolean(1);   // number → boolean
console.log(bool);

let s = String(100);     // number → string
console.log(s);